<h1>Editing widgets</h1>

<% form_for(@widgets) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @widgets %> |
<%= link_to 'Back', widgets_path %>